{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "tasks": {
    "format": {},
    "lint": {
      "dependsOn": ["^lint"]
    },
    "lint:fix": {
      "dependsOn": ["^lint:fix"]
    }
  }
}
